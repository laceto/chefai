"""
chefai — top-level package.

Modules:
    parser        — PDF recipe extraction pipeline (requires PyMuPDF, tqdm)
    extractor     — Markdown recipe extraction pipeline (stdlib only)
    feed_embedder — FAISS vectorstore helpers for RSS feed article embeddings
"""

__version__ = "0.1.0"
